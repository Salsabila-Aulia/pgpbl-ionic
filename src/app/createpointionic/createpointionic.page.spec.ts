import { ComponentFixture, TestBed } from '@angular/core/testing';
import { CreatepointionicPage } from './createpointionic.page';

describe('CreatepointionicPage', () => {
  let component: CreatepointionicPage;
  let fixture: ComponentFixture<CreatepointionicPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(CreatepointionicPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
